<%= render "shared/header" %>


<% if user_signed_in? %>
  <div class="body-wrap">
    <div class="top-report-area">
      <%= render "rooms/report_bar" %>
    </div>
    <div class="search-wrap">
      <%= render "rooms/search_bar" %>
    </div>
    <%= render "shared/rooms"%>
  </div>
<% else %>
  <div class="top-pic-area">
    <div class="top-pic-mask">

      <div class="btn-wrap">
        <div class="lower-btn">
          <div class="one-btn">  
            <%= link_to "ログイン", new_user_session_path, method: :get, class:"top-sign-in btn" %>
          </div>
          <div class="one-btn"> 
            <%= link_to "新規登録", new_user_registration_path, method: :get, class:"top-sign-up btn" %>
          </div>
        </div>
        <div class="lower-btn">
          <div class="one-btn">
            <%=  form_for(User.new, url:user_session_path) do |f| %>
              <%= f.hidden_field :email, value: "guest@example.com" %>
              <%= f.hidden_field :password, value: "guest111" %>
              <%= f.submit "かんたんログイン", class:"top-easy-in btn" %>
              <p>採用ご担当者様用</p>
            <% end %>
          </div>
          <div class="one-btn">
            <%=  form_for(User.new, url:user_session_path) do |f| %>
              <%= f.hidden_field :email, value: "dispatch@example.com" %>
              <%= f.hidden_field :password, value: "dispatch11" %>
              <%= f.submit "派遣ログイン", class:"dispatch-easy-in btn" %>
              <p>派遣・スポット用</p>
            <% end %>
          </div>
      </div>

      <div class="top-message-wrap">
        <p class="top-message-title">- sup-appとは -</p>
        <p class="top-message-content">sup-appは、介護事業所に勤務する介護士のためのアプリケーション。介護に必要な「お客様情報管理」「記録」「報告書作成」「職員同士のチャット」が、１つのアプリで行えます。</p>
      </div>

    </div>
  </div>
<% end %>

<%= render "shared/footer" %>